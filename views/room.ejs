<%- include("partials/header") %>

<div class="container mt-4 bg-light">
				<%if(roomdata.roundnumber == 1 && user.type == "Admin"){ %>
				<div class="card text-white bg-warning my-2" style="max-width: 70rem;">
				  <div class="card-header">Admin indications</div>
				  <div class="card-body">
					<ul class="card-text">
						<li>On the right side, is a button named new letter, click on button to generate a new letter and notified the players  to refresh the page. </li>
						<li>The generated letter will be display on the middle by side of the letter text</li>
					</ul>
				  </div>
				</div>
				<%}%>
				<%if(roomdata.roundnumber == 1 && user.type == "User"){ %>
				<div class="card text-white bg-warning mb-3 my-2" style="max-width: 70rem;">
				  <div class="card-header">User indications</div>
				  <div class="card-body">
					<ul class="card-text">
						<li>The generated letter will be display on the middle by side of the letter text which is generated by the admin of the room </li>
					</ul>
				  </div>
				</div>
				<%}%>
	<div class="d-inline mt-4">
		<div class="form-row d-flex">
				  <div class="col-md-5 ml-auto p-2">
					  <div class="btn-group" role="group" aria-label="Group content">
						  <button type="button" class="btn btn-secondary"><%= user.name %></button>
						  <button type="button" class="btn btn-secondary"><%= user.type %></button>
						  <button type="button" class="btn btn-secondary"> Room <span class="badge badge-light"># <%= user.roomid %></span></button>
						  <button type="button" class="btn btn-secondary"> Players <span class="badge badge-light"># <%= roomdata.players %></span></button>
					  </div>
				  </div>
				  <div class="col-md-4 ml-auto p-2">	
					<p>Letter: 
						<span>
							<a id="letter" href="#" class="btn btn-secondary border border-success disabled bg-dark" role="button"><%= roomdata.character %></a>
						</span>
					</p>
				  </div>
				  <div class="col-md-3 ml-auto p-2">
					<div class="d-inline">
					  <a href="#" class="btn btn-dark disabled" role="button">Score <%= user.totalscore %></a>
					  <a type="button" class="btn btn-secondary"> Round <span class="badge badge-light"># <%= roomdata.roundnumber %></span></a> 
					</div> 
					  <%if(user.type == "Admin"){%>
					  <form action="/char/<%= roomdata._id %>/<%= user._id %>" method="POST">
						   <button class="btn btn-outline-dark mt-2">New letter</button>
					  </form>
					<%}%> 
				  </div>
			</div>	
	</div>
		<div>
		<form action="/room/<%= roomdata._id %>/<%= user._id %>" method="POST">	
		<table class="table table-striped">
		  <thead>
			<div class="form-row">
				<tr>
				  <div class="col-md-3">
				  	<th scope="col"><label for="color">Color</label></th>
				  </div>
				  <div class="col-md-3">	
				  	<th scope="col"><label for="Name">Name</label></th>
				  </div>
				  <div class="col-md-3">	
				  	<th scope="col"><label for="city">City</label></th>
				  </div>
				  <div class="col-md-3">	
				  	<th scope="col"><label for="fruit">Fruit</label></th>
				  </div>
				</tr>
			</div>
		  </thead>	
		  <tbody>
			  <tr>
			  <div class="col-md-3">
				  <td><input type="text" class="form-control" name="color" placeholder="Enter color"></td>
			  </div>
			  <div class="col-md-3">
				  <td><input type="text" class="form-control" name="Name" placeholder="Enter name"></td>
			  </div>
			  <div class="col-md-3">
				  <td><input type="text" class="form-control" name="city" placeholder="Enter City"></td>
			  </div>
			  <div class="col-md-3">
				  <td><input type="text" class="form-control" name="fruit" placeholder="Enter Fruit"></td>
			  </div>
			  </tr>
		  </tbody>
		</table>
			<button class="btn btn-outline-dark">Save</button>
			<a class="btn btn-outline-dark" href="/room/<%=roomdata._id %>/<%=user._id %>">Reload page</a>
		</form>
		</div>
	<div>
	</div>
</div>

<%- include("partials/footer") %>
